# SQLite file, resolved relative to the prisma/ directory.
DATABASE_URL="file:./dev.db"

# Auth.js session signing key. Replace this before any shared deployment.
AUTH_SECRET="dala-dev-secret-change-me"
AUTH_TRUST_HOST=true
